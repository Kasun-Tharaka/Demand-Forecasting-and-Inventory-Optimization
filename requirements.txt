# Recommended: create a conda env with Python 3.11 for best binary compatibility
# Core data stack
numpy==1.26.4
pandas==2.2.2
polars==0.20.31
# faster alternative for large melts

# Parquet I/O
pyarrow==16.0.0
fastparquet==2024.2.0

# Machine Learning (Phase 4)
lightgbm==4.3.0
scikit-learn==1.4.2
scipy==1.13.0

# Optimization (Phase 5)
# scipy already listed above — minimize, linprog

# Visualization
matplotlib==3.8.4
seaborn==0.13.2
plotly==5.22.0

# Notebook / reporting
jupyterlab==4.1.6
ipykernel==6.29.4
tqdm==4.66.2
# progress bars for long loops

# Utilities
python-dotenv==1.0.1
pyyaml==6.0.1
loguru==0.7.2
# structured logging
psutil==5.9.8
# RAM monitoring
memory-profiler==0.61.0
# @profile decorator

# Optional GPU acceleration
# cudf-cu12==24.4.0       # RAPIDS — uncomment if CUDA GPU available
# cupy-cuda12x==13.1.0
# Note: these packages often fail on Windows or without the exact CUDA/cuDNN setup.